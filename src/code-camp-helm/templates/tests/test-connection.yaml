apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "code-camp-helm.fullname" . }}-test-connection"
  labels:
    {{- include "code-camp-helm.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "code-camp-helm.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
